import { StyledHeader } from "@/styles/styled-header";

export const Header = () => {
	return (
		<StyledHeader>
			<button>Início</button>
			<button>Projetos</button>
			<button>Sobre</button>
		</StyledHeader>
	);
};
